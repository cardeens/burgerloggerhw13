<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR IT!{{else}}BRING IT BACK!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">REMOVE!</button>
</li>
